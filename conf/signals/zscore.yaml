# Z-Score Signal Configuration

zscore:
  window: 252           # Rolling window in trading days (1 year)
  threshold: 7.0        # Trigger when |z| > threshold
  min_observations: 126 # Minimum observations before computing (6 months)
  ddof: 1               # Degrees of freedom for std calculation
